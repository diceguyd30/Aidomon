name: Run Unit Tests
on: push
jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: MikeSchulze/gdUnit4-action@v1
        with:
          godot-version: '4.2.1'
          paths: 'res://test'
          upload-report: false
